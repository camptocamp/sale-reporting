This Module adds a setting in General Settings to set currency which be applied to all companies (or one company if only one exists) as Group currency.
Then you can install `sale_group_currency` from https://github.com/OCA/sale-reporting or `crm_group_currency` from https://github.com/OCA/crm to have additional Total (Group Currency) field.
